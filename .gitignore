# Environment files — never commit these
# (*.example files are templates and ARE safe to commit)
backend/.env
.env
.env.local
frontend/.env
frontend/.env.local
.env.*.local

# Python
__pycache__/
*.pyc
*.pyo
.pyo
.venv/
venv/
*.egg-info/
dist/
*.egg

# Tests / coverage
.pytest_cache/
.coverage
coverage.xml
htmlcov/
.hypothesis/

# Node
node_modules/
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/.turbo/
frontend/build/

# TypeScript build artifacts
*.tsbuildinfo
tsconfig.tsbuildinfo

# Next.js generated files (auto-rebuilt on every dev/build run)
frontend/next-env.d.ts

# Vercel deployment config
.vercel/

# Database
backend/*.db
*.db
*.sqlite
*.sqlite3

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Secrets / certs
*.pem
*.key
*.p12
*.pfx

# OS
.DS_Store
Thumbs.db
desktop.ini
.AppleDouble
.LSOverride

# Stripe CLI binaries (local dev tool — not part of source)
stripe.exe
stripe

# Claude Code local project files
.claude/
CLAUDE.md

# Brand assets (tracked via other means or not needed in repo)
Vista Logos/

.vercel
